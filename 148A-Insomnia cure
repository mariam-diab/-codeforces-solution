#include<iostream>
using namespace std;
#define FAST_IO ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);

int main(){
    FAST_IO;
    int k, l, m, n, d, t=0; cin>>k>>l>>m>>n>>d;
    for (int i=1; i<=d; i++) t+=(i%k && i%l && i%m && i%n);
    cout<<d-t;

}
